import React from 'react';
import { connect } from 'react-redux';

const Home = props => (
  <div>
    <h1>Ramsay Technical Test</h1>
    <p>by <b>By Felix Sebastian RIncon Tobo</b></p>
    
    </div>
);

export default connect()(Home);
